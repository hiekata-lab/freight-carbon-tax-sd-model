# SD Modelling of Carbon Tax in Commercial Vehicle Sector

```bash
python3 -m venv venv
pip install -r requirements.txt
source venv/bin/activate

# Translates latest .mdl to .py
python src/translate_model.py
```
